# acp-connector

Thin bridge connecting messaging platforms (Telegram first) to any ACP-compatible coding agent via the Agent Client Protocol.

## Install

```
npx acp-connector setup
```

## Run

```
npx acp-connector
```

## Config

Configuration lives in `.config.jsonc` in the current working directory. See `.config.example.jsonc` for all options.
