# BEHAVIOR.md
> This file is generated by context-organizer. Do not edit it manually.

## Module: AcpClient edge cases
- default permission handling with no options array returns cancelled
- default permission handling with options but no allow option returns cancelled
- default-approves permission when no handler is set and allow option exists
- delegates to onPermission handler that cancels
- handles agent closing stdin unexpectedly without crashing
- listSessions before start throws
- loadSession before start throws
- loadSession disposes old session before switching
- newSession before start throws
- newSession with sessionConfig uses config-based builder
- nextUpdate() after kill() does not hang or crash
- prompt() before start() throws a clear error
- prompt() forwards empty text to the session
- prompt() forwards very long text to the session
- rejects start() when session/load throws
- rejects start() when session/resume throws
- rejects start() when the child process emits an error (command not found)
- second kill() is safe and does not throw
- second start() is safe and does not spawn a second process

## Module: AcpClient
- auto-approves permission when no onPermission callback
- calls session/load when sessionId provided and loadSession capability is true
- calls session/resume when resume capability is available
- creates a new session when no sessionId provided
- delegates permission to onPermission callback
- does not call set_mode when no sessionMode configured
- initial session mode failure is non-fatal (agent does not support modes)
- kill() disposes session and kills process
- listSessions returns sessions when capability supported
- listSessions throws when capability not supported
- loadSession throws when no load/resume capability
- loadSession uses session/load when only loadSession capability is true
- loadSession uses session/resume when resume capability is available
- loads session config from sessionConfigPath
- newSession creates a new session and updates sessionId
- nextUpdate() delegates to session.nextUpdate
- prompt() delegates to session.prompt
- setSessionMode calls session/set_mode with correct params
- setSessionMode throws when no active session
- sets initial session mode after loadSession
- sets initial session mode after newSession
- sets initial session mode after start when sessionMode provided
- stores agentCapabilities from init response
- throws when no load/resume capability and sessionId provided

## Module: BridgeBot edge cases
- auto-approves permission with no chatId
- auto-approves when agentCmd contains "bypass"
- auto-approves when agentCmd contains "dangerous"
- auto-approves when agentCmd contains "yolo"
- double stop is safe
- falls back to plain text when markdown fails
- filters thoughts when showThoughts is false
- forwards thoughts when showThoughts is true
- forwards unknown /command from enqueuePrompt to agent
- handles /help from enqueuePrompt (HTTP path)
- handles /start from enqueuePrompt (HTTP path)
- handles agent crash during prompt
- handles callback query with invalid data
- handles callback query with no pending permission
- handles empty text message
- handles permission with no options
- handles setup mode (empty allowedChatIds)
- handles stream with only stop (no content)
- queues multiple rapid messages
- rejects document messages
- rejects photo messages
- rejects sticker messages
- rejects unauthorized chat
- rejects voice messages
- splits long output over 4096 chars

## Module: BridgeBot
- /mode <id> switches mode
- /mode <unknown> sends available modes
- /mode lists available modes
- /mode on error sends error message
- /mode when no modes available sends message
- /new on error sends error message
- /new when busy refuses with message
- /new when idle creates new session
- /session <id> switches session
- /session when busy refuses with message
- /session without id sends usage
- /sessions lists available sessions
- /sessions when not supported sends error
- /stop clears the queue
- /stop on cancel error sends error message
- /stop when busy cancels the agent
- /stop when idle responds nothing to stop
- auto-approves permission when agentCmd includes dangerous
- delegates bridge commands to onCommand
- enqueuePrompt adds to queue and processes
- enqueuePrompt with onComplete calls it with error on failure
- enqueuePrompt with onComplete calls it with response after processing
- falls back to plain text on markdown parse error
- forwards thoughts when showThoughts=true
- forwards to agent when onCommand returns false
- handles agent_message (full message)
- ignores thoughts when showThoughts=false
- queues and forwards text to ACP
- rejects non-text messages
- rejects unauthorized chat ID
- responds with chat ID in setup mode (empty allowlist)
- sends error message on prompt failure
- sends error message when acp.prompt() rejects
- sends permission buttons when agentCmd has no dangerous
- sends stop reason placeholder when no output
- splits output >4096 chars into multiple messages
- starts and sets up handlers
- stop() stops polling
- streams agent_message_chunk and edits single message

## Module: bridge
- exits with error when ACP fails to start
- exits with error when no config found
- preserves Discord Snowflake channel IDs for cron allowedChatIds
- routes permission to the bot with an active prompt (Discord), not the first bot (TG)
- starts ACP, bot, cron, and HTTP
- wires onCommand to routine manager
- wires onPermission to bot

## Module: config edge cases
- empty file (0 bytes) returns null instead of throwing
- extra unknown fields are preserved safely
- file with block comments is parsed
- file with line comments is parsed
- file with only comments returns null instead of throwing
- file with only whitespace returns null
- file with trailing comma (invalid JSONC) is accepted
- invalid JSON (syntax error) throws a clear, rethrown error
- legacy telegramToken without platforms migrates to platforms format
- loadConfig on non-existent path returns null
- missing allowedChatIds in platforms.telegram defaults to empty array
- missing required fields (no agentCmd) throws a validation error
- missing required fields (no platforms or telegramToken) throws a validation error
- saveConfig then loadConfig round-trips a valid config
- saveConfig with empty object writes a file that loads as null
- saveConfig with null/undefined throws a clear error
- saveConfig writes to a path inside a non-existent nested dir fails predictably

## Module: config
- defaultConfigPath resolves to cwd
- does not strip // inside strings
- loads a simple config
- returns null when no config file exists
- saveConfig writes valid JSON
- strips block comments
- strips line comments
- strips trailing commas

## Module: CronManager edge cases
- add() after stop() starts the new job
- add() rejects duplicate name and does not push a second job
- multiple jobs with same schedule all start
- remove() returns false for non-existent job
- run() returns false for non-existent job
- skips job with empty prompt
- skips job with empty schedule
- skips job with invalid cron expression
- skips job with no chatId when allowedChatIds is empty
- start() with empty jobs array is a no-op
- stop() when no jobs running is a no-op
- toggle() returns false for non-existent job

## Module: CronManager
- add() rejects duplicate name
- add() starts a new job
- fires job and enqueues prompt
- list() returns all jobs
- remove() returns false for unknown job
- remove() stops and removes job
- run() executes job immediately
- run() returns false for unknown job
- skips disabled jobs
- skips invalid schedules
- skips jobs with no chatId when allowedChatIds is empty
- starts all valid jobs
- stop() stops all tasks
- toggle() pauses and activates
- uses job chatId when provided

## Module: DiscordBot
- /stop clears the queue
- /stop when busy cancels the agent
- /stop when idle responds nothing to stop
- auto-approves permission when agentCmd includes dangerous
- auto-approves when no current channel
- cancels permission when no options
- enqueues prompts from external sources
- forwards allowed messages to ACP
- forwards thoughts when showThoughts=true
- handles /start and /help commands
- handles agent_message (non-chunk)
- handles onCommand callback
- ignores bot messages
- ignores empty text
- ignores thoughts when showThoughts=false
- rejects unauthorized channel ID
- responds with channel ID in setup mode (empty allowlist)
- sendMessage sends to the channel
- sends error message on prompt failure
- sends error message when acp.prompt() rejects asynchronously
- sends permission buttons when channel is active
- sends stop reason when response is empty
- starts and logs in
- stop() destroys the client
- streams agent response chunks

## Module: HttpServer edge cases
- Authorization header is never forwarded to agent
- GET /health does not call enqueue
- GET /health with auth token configured requires auth
- GET /unknown route returns 404
- POST /prompt backward compat: no files field works as before
- POST /prompt with correct bearer token enqueues
- POST /prompt with empty text returns 400
- POST /prompt with extra fields ignores them and enqueues text
- POST /prompt with files but no text still enqueues
- POST /prompt with files missing data is skipped
- POST /prompt with image file passes ContentBlocks
- POST /prompt with invalid JSON uses raw body as prompt
- POST /prompt with large body (>10KB) is accepted
- POST /prompt with multiple files creates multiple blocks
- POST /prompt with no body returns 400
- POST /prompt with non-image file creates resource_link
- POST /prompt with non-string text uses raw body as prompt
- POST /prompt with query params adds context to prompt
- POST /prompt with raw body + query params adds context
- POST /prompt with text but no chatId enqueues with undefined
- POST /prompt with whitespace-only text returns 400
- POST /prompt with wrong bearer token returns 401
- POST /prompt without auth token when configured returns 401
- POST /prompt without callback_url does not pass onComplete
- POST /prompt without query params sends text as-is
- POST /unknown route returns 404
- body exceeding maxBodySize returns 413
- double stop() is idempotent and does not throw
- forwardHeaders disabled by default
- forwardHeaders enabled includes custom headers in prompt
- health check with agent session returns session info
- health check with no agent session (default getHealth) returns 200 ok
- onComplete callback fires with error when agent fails
- onComplete callback fires with response when agent finishes
- rate limit returns 429 after exceeding limit
- server does not start when enabled=false

## Module: HttpServer security tests
- DELETE /prompt returns 404
- GET /health with correct auth returns 200
- GET /prompt returns 404 (not 405)
- POST /prompt/extra returns 404
- PUT /prompt returns 404
- accepts token with special characters
- all responses are JSON
- auth + query params + forward headers all work together
- error responses include error field
- handles JSON array body as raw prompt
- handles JSON null body as raw prompt
- handles JSON number body as raw prompt
- handles JSON string body as raw prompt
- handles URL-encoded query params
- handles body with newlines
- handles body with unicode
- handles empty query param values
- handles many query params
- handles no content type
- handles text/plain content type
- rate limit counts all requests including /health
- rate limit resets after entries expire
- rate limit with auth still counts unauthorized requests
- raw body + query params + auth all work together
- rejects Basic auth scheme even with correct token
- rejects empty bearer token
- rejects lowercase bearer scheme
- rejects path traversal in URL
- rejects token with extra spaces
- rejects very long token (DoS attempt)

## Module: HttpServer
- GET /health returns 200 with health info
- POST /prompt enqueues a prompt
- POST /prompt with JSON body but no text field uses raw body as prompt
- POST /prompt with callbackUrl (camelCase) passes onComplete to enqueue
- POST /prompt with callback_url passes onComplete to enqueue
- does not start when enabled is false
- stop() closes the server
- unknown route returns 404

## Module: MediaHandler
- adds caption as text block
- creates ImageContent when agent supports image
- creates ResourceLink for non-image files
- creates ResourceLink when agent does not support image
- creates uploads dir if it does not exist
- processMedia downloads, saves, and converts
- saves file to disk

## Module: BridgeBot media handling
- downloads and processes document messages
- downloads and processes photo messages
- falls back to ResourceLink when agent does not support image
- handles sticker messages
- rejects media when no mediaHandler configured
- sends error message on download failure

## Module: RoutineManager edge cases
- /cron add with 4-token schedule fails (needs 5)
- /cron add with multi-word prompt works
- /cron add with no prompt (only 5 tokens) fails
- /cron add with no schedule (empty) fails
- /cron remove with empty name shows usage
- /cron run with empty name shows usage
- /cron toggle with empty name shows usage
- /routine add with duplicate name rejects
- /routine add with no name shows usage
- /routine add with no prompt shows usage
- /routine remove with empty name shows usage
- /run with empty name shows usage
- _persist does nothing when loadConfig returns null

## Module: RoutineManager
- /cron add creates job and persists
- /cron list shows all jobs
- /cron remove stops job and persists
- /cron run executes job immediately
- /cron toggle toggles and persists
- /cron with no subcommand shows help
- /routine add creates and persists
- /routine add rejects duplicate name
- /routine list shows all routines
- /routine list shows empty message when no routines
- /routine remove deletes and persists
- /routine remove with unknown name returns error
- /routine with no subcommand shows help
- /run executes routine by name
- /run with no args shows usage
- /run with unknown name returns error
- returns false for non-command text
- returns false for unknown command
